import * as _generic from './generic';
import * as _object from './object';
import * as _string from './string';
import * as _slippage from './slippage';

const config = {
    generic: _generic,
    object: _object,
    string: _string,
    slippage: _slippage,
}

export default config;